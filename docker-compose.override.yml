services:
  frontend:
    build:
      context: ./frontend
      target: builder
    command: npm run dev -- --host
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app:z
      - /app/node_modules
